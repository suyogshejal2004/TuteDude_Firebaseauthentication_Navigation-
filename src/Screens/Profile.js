import { Text, View } from "react-native";
import { Layout } from "react-native-rapi-ui";

const Profile = () => {
  return (
    <Layout>
      <View>
        <Text>Profile</Text>
      </View>
    </Layout>
  );
};
export default Profile;
